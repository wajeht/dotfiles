# This is `bat`s configuration file. Each line either contains a comment or
# a command-line option that you want to pass to `bat` by default. You can
# run `bat --help` to get a list of all possible configuration options.

# Specify desired highlighting theme (e.g. "TwoDark"). Run `bat --list-themes`
# for a list of all available themes
--theme="Visual Studio Dark+"

# Always show colors, even when piping to other commands
--color=always

# Enable this to use italic text on the terminal. This is not supported on all
# terminal emulators:
#--italic-text=always

# Uncomment the following line to disable automatic paging:
--paging=never

# Uncomment the following line if you are using less version >= 551 and want to
# enable mouse scrolling support in `bat`. This might disable text selection,
# unless you press shift.
#--pager="less --RAW-CONTROL-CHARS --quit-if-one-screen --mouse"

# Show line numbers
--number

# Show Git modifications in the gutter (only if inside a Git repository)
--decorations=always

# Wrap text instead of using horizontal scrolling
--wrap=auto

# Show non-printable characters like space, tab etc.
# --show-all

# Use custom tab width
--tabs=2

# Syntax mappings: map a certain filename pattern to a language.
#   Example 1: use the C++ syntax for .ino files
#   Example 2: Use ".gitignore"-style highlighting for ".ignore" files
--map-syntax "*.ino:C++"
--map-syntax ".ignore:Git Ignore"
--map-syntax "Brewfile:Ruby"
--map-syntax "*.conf:INI"
--map-syntax "*.env:Bourne Again Shell (bash)"
--map-syntax "*.zshrc:Bourne Again Shell (bash)"
--map-syntax "*.bashrc:Bourne Again Shell (bash)"
--map-syntax "install.sh:Bourne Again Shell (bash)"
--map-syntax "Makefile:Makefile"
--map-syntax "*.go:Go"
--map-syntax "*.php:PHP"
--map-syntax "*.txt:Plain Text"
--map-syntax "*.toml:TOML"
--map-syntax "*.json:JSON"
--map-syntax "*.js:JavaScript"
--map-syntax "*.ts:TypeScript"
--map-syntax "*.vue:Vue.js Component"
